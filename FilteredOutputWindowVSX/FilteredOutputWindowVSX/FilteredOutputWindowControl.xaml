<UserControl x:Class="FilteredOutputWindowVSX.FilteredOutputWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             xmlns:s="clr-namespace:System;assembly=mscorlib"
             d:DesignHeight="300" d:DesignWidth="500"
             xmlns:tools="clr-namespace:FilteredOutputWindowVSX.Tools"
             Name="MyToolWindow">
    <UserControl.Resources>
        <tools:BoolToVisConverter x:Key="BoolToVisibility"/>
        <s:Boolean x:Key="True">True</s:Boolean>
        <s:Boolean x:Key="False">False</s:Boolean>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel>
            <ToolBarTray DockPanel.Dock="Top">
                <ToolBar>
                    <Button MaxHeight="50" 
                            Command="{Binding StartRecording}"
                            Name="StartListeningButton" 
                            Visibility="{Binding IsRecording, Converter={StaticResource BoolToVisibility}, ConverterParameter={StaticResource False}}">
                        <!--<Image Source="Resources/starticon.png" ></Image>--> Start
                    </Button>
                    <Button MaxHeight="50" 
                            Command="{Binding StopRecording}"
                            Name="StopListeningButton" 
                            Visibility="{Binding IsRecording, Converter={StaticResource BoolToVisibility}, ConverterParameter={StaticResource True}}" >
                        <!--<Image Source="Resources/stopicon.png" ></Image>--> Stop
                    </Button>
                    <Button Name="CleanButton" 
                            Command="{Binding Clear}"
                            >
                        Clear
                    </Button>
                    <CheckBox IsChecked="{Binding AutoScroll}"
                              Name="ScrollToEndCheck">
                        Autoscroll
                    </CheckBox>
                    <Separator/>
                    <Label>Tags in CSV format: </Label>
                    <TextBox Name="tagsBox" 
                             Text="{Binding Tags}"
                             HorizontalAlignment="Stretch" 
                             Width="200" 
                             VerticalContentAlignment="Center"/>
                </ToolBar>
            </ToolBarTray>
            <Label Content="{Binding Output}"
                   HorizontalAlignment="Stretch"
                   VerticalAlignment="Stretch"
                   Padding="10">
            </Label>
            <!--<RichTextBox VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Name="outputBox" >
                <RichTextBox.Resources>
                    <Style TargetType="{x:Type Paragraph}">
                        <Setter Property="Margin" Value="1"/>
                    </Style>
                </RichTextBox.Resources>
            </RichTextBox>-->
        </DockPanel>
    </Grid>
</UserControl>
