<UserControl x:Class="FilteredOutputWindowVSX.FilteredOutputWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             xmlns:controls="clr-namespace:FilteredOutputWindowVSX.Controls"
             xmlns:s="clr-namespace:System;assembly=mscorlib"
             d:DesignHeight="300" d:DesignWidth="500"
             xmlns:tools="clr-namespace:FilteredOutputWindowVSX.Tools"
             Name="MyToolWindow">
    <UserControl.Resources>
        <tools:NullToHiddenConverter x:Key="NullToHiddenConverter"/>
        <tools:BoolToVisConverter x:Key="BoolToVisibility"/>
        <tools:RevertBoolConverter x:Key="RevertBool"/>
        <s:Boolean x:Key="True">True</s:Boolean>
        <s:Boolean x:Key="False">False</s:Boolean>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel>
            <ToolBarTray DockPanel.Dock="Top">
                <ToolBar>
                  
                    
                    <Label>Filter</Label>
                    <ComboBox ItemsSource="{Binding Filters}" SelectedItem="{Binding CurrentFilter}" Width="200" DisplayMemberPath="Name"></ComboBox>
                    <Button Height="25" Name="AddNewFilter" 
                            Command="{Binding AddNewFilter}"
                            >
                        Add
                        <!--<Image Source="Resources/add.png" >
                        </Image>-->
                    </Button>
                    <Button Height="25" Name="EditFilter" 
                            Command="{Binding EditFilter}" Visibility="{Binding CurrentFilter, Converter={StaticResource NullToHiddenConverter}}"
                            >
                        Edit
                        <!--<Image Source="Resources/pencil.png" >
                        </Image>-->
                    </Button>
                    <Button Height="25" Name="DeleteFilter" 
                            Command="{Binding DeleteFilter}" Visibility="{Binding CurrentFilter, Converter={StaticResource NullToHiddenConverter}}"
                            >
                        Delete
                        <!--<Image Source="Resources/trash.png" >
                        </Image>-->
                    </Button>
                    <Separator/>
                    <Button ToolTip="Clear" Height="25" Name="CleanButton" 
                            Command="{Binding Clear}"
                            IsEnabled="{Binding IsRecording, Converter={StaticResource RevertBool}}"
                            >
                        Clear
                        <!--<Image Source="Resources/clear.png" >
                        </Image>-->
                    </Button>

                    <CheckBox IsChecked="{Binding AutoScroll}"
                              Name="ScrollToEndCheck">
                        Autoscroll
                    </CheckBox>
                </ToolBar>
            </ToolBarTray>
            <controls:FilterBuilder  Height="70" DockPanel.Dock="Top" HorizontalAlignment="Stretch" Visibility="{Binding EditingFilter, Converter={StaticResource NullToHiddenConverter}}"></controls:FilterBuilder>
            <ScrollViewer Name="Scroller" Tag="{Binding AutoScroll}" VerticalScrollBarVisibility="Auto">
                <TextBox Name="Output" Text="{Binding Output}"
                         IsReadOnly="True"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch"
                         Padding="10"
                         />
                         <!--Foreground="White"-->
            </ScrollViewer>
        </DockPanel>
    </Grid>
</UserControl>
